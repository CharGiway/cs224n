# TODO

## 验证正弦位置编码的作用

通过 `attn_vanilla/` 中的 Transformer 模型，验证以下说法是否成立：

### 核心论点

位置编码（sinusoidal PE）不是 "embedding + 一个小噪声"，而是：

```
embedding + 隐藏信号
      ↓
 经过可学习变换 (W_Q, W_K, W_V)
      ↓
 变成明显的位置特征
```

### 直觉解释

position 信息经过矩阵 `W_Q` 后可以被放大。

举例：
- 输入 `p_i W_Q` 可能从微小差异变成 `[5.2, -3.1, 8.7, ...]`
- 因为 `W_Q` 是训练出来的，不是随机的

更直观地：
```
猫 = [1, 0, 0]
狗 = [0, 1, 0]

加上位置：
位置1 = [0.001, 0.001, 0.001]
位置2 = [0.002, 0.002, 0.002]
```

看起来微小差异会淹没在语义嵌入中，但网络训练过程中发现：

> "这个方向上的微小变化和任务有关。"

于是它会学：**那个维度 × 1000**，然后位置差异就被放大出来了。

### 待验证

- [ ] 训练 `attn_vanilla` 模型，提取 `W_Q` 权重，可视化位置编码经过 `W_Q` 后的变化幅度
- [ ] 对比：去掉位置编码 vs 使用正弦位置编码的训练效果差异
- [ ] 分析不同层、不同 head 对位置信息的敏感度
